name: Deploy AI Stack

on:
  push:
    branches:
      - main

jobs:

  deploy:

    runs-on: self-hosted

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy Stack
        run: |
          cd /opt/ai-stack
          git pull
          docker compose pull
          docker compose up -d
